# 變更紀錄

格式參考 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，版本遵循 [SemVer](https://semver.org/lang/zh-TW/)。
這是內部實驗專案（`private: true`，未發佈至 npm）；版本號用來標記「哪一代」，逐項變更以 git log 為準。

## [0.2.0] — 2026-07-27

WiFi 指紋室內定位的**離線工具鏈**。本代不改變 viewer 行為——定位引擎尚未接進 app。

### 新增

- **指紋模擬器**（`npm run sim:fp`）：在還沒到現場採集前，用 seeded 物理模型產生合成的
  `wifi-fp@1` 採集檔，讓 pipeline 可以先寫完測完。模型含距離衰減、跨樓層衰減、持久遮蔽、
  人體遮擋、偵測閃爍與磁場梯度，並可依比例注入髒資料（節流、短掃描、轉動、低磁力精度、重採）。
  校準對齊實測表（朝向差約 12 dB、10 批 span 約 16 dB、每批 6–15 顆 AP）。
- **離線 pipeline**（`npm run build:fp`）：採集檔（真的或合成的）→ 指紋庫 `fp-db@1` JSON。
  含 Stage 1 清洗（樣本品質過濾、行動熱點三規則濾除、多 BSSID 同源合併）與 Stage 2 建庫
  （加權統計、每點 Top-15、磁力指紋）。702 點實測產出 393 KB／gzip 64 KB，前端可整包載入。
- **`src/fp/core.ts`**：指紋數學（`fingerprint` / `fpDistance`）從 CLI 抽出成共用模組，
  供未來在瀏覽器執行的定位引擎 import。
- **真機採集 fixture**（`tests/fixtures/real-home/`）：2026-07-26 家中 8 點實採的匿名化版本，
  保留 MAC 位元關係、移除真實 OUI 與鄰居 SSID。搭配 `tests/fp-real.test.ts` 的特徵測試，
  其中兩條刻意標記【已知缺口】，追蹤「衍生 MAC 未合併、佔 Top-15 的 16.7%」這個待裁定的問題。
- **`docs/adr/`**：專案第一份架構決策紀錄（ADR 0001：錨點的定義是「一份獨立證據」而非
  「一台實體 AP」）。

### 修正

- **Stage 1 轉動判別改用軸向／合力比值**。原規則是「軸向 std > 3 且合力 magStd < 2」，
  但殘留硬鐵偏移會讓合力也隨轉動超標（真機實測 P01 軸 10.16／合 2.75、P07 軸 18.70／合 3.95），
  導致大幅轉動落進「環境擾動」分支、**轉動污染的 WiFi 被以全權重納入指紋庫**。
  改為「軸向 > 3 且軸向 > 合力×2」，與採集 APK 的 `magQuality()` 同一條判別式。
- 採集 APK 的同一問題（`fix: 磁力品質判別改用軸向/合力比值`）。兩邊必須一起改。

### 變更

- **錨點（anchor）的定義**：從「多 BSSID 合併後的實體 AP」改為「指紋比對時算作一份獨立證據的
  訊號來源」。合併判準因此降級為代理指標，驗收標準改為「併或不併，看有沒有讓評分更準」。
  理由與被否決的替代方案見 ADR 0001。
- `tools/analyze-spacing.ts` 改為 import `src/fp/core`，CLI 行為不變。

### 文件

- README 指令表補上 `gen:rp`／`analyze:spacing`／`sim:fp`／`build:fp`（前兩者已漏一代），
  並分為開發、資料、指紋三組；目錄段補上 `src/fp/`、`tools/wifi-collector/`、`rp/`、`docs/adr/`。
- `docs/data-conventions.md` 新增「WiFi 指紋增補慣例」，記錄 `fp-db@1` 的形狀與輸出路徑、
  清洗門檻常數作為調參旋鈕的用法，以及「模擬器只驗程式、不預測真實誤差」的界線。
- `docs/wifi-collector-guide.md` **更正**疑難排解表：原本寫轉動時「WiFi 不受影響」，
  真機資料顯示同一顆 AP 前後半批可差 13 dB。照舊敘述會讓採集者不去重採受污染的點。

### 已知限制

- 定位引擎（Stage 3）尚未實作；指紋庫目前只有合成版與家中測試版，北車現場尚未採集。
- 多 BSSID 同源合併的判準兩個方向都不完備（連號 MAC 可能過度合併、衍生 MAC 合併不足），
  待北車真實資料再裁定。以特徵測試追蹤中。

## [0.1.0] — 2026-07-17 ~ 2026-07-26

專案自建立起至 WiFi 採集器完成為止的所有工作，共約 240 個 commit，期間未切分版本號。
涵蓋 Phase 1–7：樓層 JSON 資料真相與 viewer、描圖工具與 GLB 雙軌輸出、導航跟隨模式與
無障礙路徑、地圖呈現與視覺單一真源、高德式風格化、3D 選點／PDR／語音、以及現場採集用的
Android APK 與參考點產生腳本。

逐項變更請見 `git log` 與 README 的 Phase 敘述——本檔自 0.2.0 起才逐版記錄。
